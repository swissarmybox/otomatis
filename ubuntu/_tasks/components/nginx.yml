- name: nginx | install
  become: yes
  apt:
    name: nginx

 - name: nginx | allow http/https connections
   become: yes
   community.general.ufw:
     rule: allow
     name: 'Nginx Full'
     state: enabled

- name: nginx | start with systemd
  systemd:
    name: nginx
    state: started
